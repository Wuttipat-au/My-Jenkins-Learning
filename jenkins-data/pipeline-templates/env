pipeline {
    agent any

    environment {
        NAME = 'Name'
        LASTNAME = 'Lastname'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NAME $LASTNAME'
            }
        }
    }
}
